- Unpack ldraw.
- Unpack model.
- Change paths and model to display in in src/view/Main.java
- Recompile Java.
- run it.

Sorry if I'm not more specific ;-)

To run from eclipse you need to add:

-Djava.library.path=/home/dmi/workspace/Lego

To the VM arguments (Run Configurations -> LegoMainClass -> Arguments -> VM Arguments)

ldraw zip was taken from:

http://www.ldraw.org/
